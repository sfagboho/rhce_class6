[defaults]
inventory = inventory
remote_user = devops
ask_pass = false
role_paths = ./roles:/usr/share/ansible/roles
collections_paths=./collections:/home/devops/.ansible/collections:/usr/share/ansible/collections

[privilege_escalation]
become = false
become_method = sudo
become_user = root
become_ask_pass = false

